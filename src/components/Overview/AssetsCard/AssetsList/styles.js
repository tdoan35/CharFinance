import { makeStyles } from '@material-ui/core';

export default makeStyles((theme) => ({
  avatarIncome: {
    color: '#fff',
    backgroundColor: 'green',
  },
  list: {
    height: '220px',
    maxHeight: '220px',
    overflow: 'auto',
  },
}));